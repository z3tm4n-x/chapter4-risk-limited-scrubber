[project]
name = "chapter4-risk-limited-scrubber"
version = "0.1.0"
requires-python = ">=3.11"
description = "Risk-limited adaptive scrubber RTL chapter artifact"

[tool.pytest.ini_options]
testpaths = ["tests"]
